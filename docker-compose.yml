version: '3.8'

services:
  cloudflare-tunnel-manager:
    image: jameswrc/cloudflare-tunnel-manager
    container_name: cloudflare-tunnel-manager
    environment:
      # - PUID=1000
      # - PGID=1000
      - HOME=/data
      - TUNNEL_NAME=
      - ACCOUNT_ID=
      - TUNNEL_ID=
      - TUNNEL_TOKEN=
      - CLOUDFLARE_API_TOKEN=
      - LOGGING_LEVEL=INFO # optional, default is INFO, possible values are DEBUG, INFO, WARNING, ERROR, CRITICAL
      - CONFIG_CHANGE_CHECK_INTERVAL=60 # optional, default is 60s, remove this to not check for file changes
    volumes:
      # config.yml file and cert.pem will be read from the ~/.cloudflared folder. You cannot change the directory path.
      - ./cloudflared:~/.cloudflared/ 
      # Dir holding all logs generated by the tunnel manager
      - ./logs:/logs/
    healthcheck:
      test: ["CMD", "sh", "-c", "[ -f /data/ready.txt ] || exit 1"] # Check if the ready.txt file exists
      interval: 30s
      timeout: 10s
      retries: 10
      start_period: 10s
    restart: always